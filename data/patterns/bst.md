Binary Search Tree (BST) Pattern
Intuition

For every node:

Left subtree < node

Right subtree > node

Use cases:

Search, insert, delete in average O(log n).

Inorder Traversal

Produces sorted order.
